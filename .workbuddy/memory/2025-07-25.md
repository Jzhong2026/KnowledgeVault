# 2025-07-25

## Workspace 功能验证与 Docker 部署

### 环境修复
- .NET 10.0 SDK 安装 (系统仅有 .NET 9) → winget 安装 10.0.302
- 数据库迁移未执行 → 删除旧库重建，Folders 表已存在
- 批处理脚本路径修正: `d:\AI\` → `E:\`

### E2E 测试结果
- Playwright workspace 测试: 9/10 通过, 1 跳过 (move doc 需 seed)
- 修复: Exit Workspace 按钮文本 "Exit" → "Exit Workspace" (workspace-mode.ts)
- 新增: Topbar Exit Workspace 按钮 (topbar.html)
- 修复: playwright.config.ts 加载 dotenv (.env)

### Bug 修复
- `ng build` SSR 模式生成 `index.csr.html` 而非 `index.html`
- server-prod.js: Express → 原生 http 模块 (避免 http-proxy-middleware v4 ESM 兼容 + Docker Hub 不可达)
- angular.json: 新增 `docker` 静态构建配置, `outputMode: "static"`

### Docker 部署
- Backend: .NET 10 Dockerfile → port 18503
- Frontend: 多阶段构建 (Node.js build + 原生 server) → port 38080
- docker-compose.yml: 网络 + 数据卷 + 自动迁移
- 容器内 seed: kvtest 用户 + TestWorkspace 项目 + 文件夹 + 文档

### 文件改动
- 新增: Dockerfile x2, docker-compose.yml, .dockerignore x3, server-prod.js, nginx.conf, appsettings.Production.json
- 修改: angular.json, playground.config.ts, workspace-mode.ts, topbar.html, e2e/workspace.spec.ts, batch files
